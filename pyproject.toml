[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qs-ai"
version = "0.1.0"
description = "AI-powered Quantity Surveying engine (BESMM4 compliant)"
authors = [{ name = "Your Name" }]
requires-python = ">=3.10"

dependencies = [
    "pandas",
    "openpyxl",
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
